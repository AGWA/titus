#!/bin/sh

set -e

if [ "$1" = "purge" ]; then

	# Remove users
	! getent passwd titus >/dev/null || deluser --quiet titus
	! getent passwd titus-keys >/dev/null || deluser --quiet titus-keys
fi

#DEBHELPER#

exit 0
